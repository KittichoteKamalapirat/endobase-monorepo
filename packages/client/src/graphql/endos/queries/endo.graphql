query Endo($id: String!) {
  endo(id: $id) {
    id
    serialNum
    dryingTime
    status
    brand
    type
    model
    lastPutBackISO
    position
    trayId
    tray {
      id
      row
      position
      container {
        id
        col
      }
    }
  }
}
