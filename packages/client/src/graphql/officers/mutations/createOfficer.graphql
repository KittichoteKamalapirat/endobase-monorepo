mutation CreateOfficer($input: CreateOfficerInput!) {
  createOfficer(input: $input) {
    officer {
      id
      officerNum
      firstName
      lastName
      type
    }

    errors {
      field
      message
    }
  }
}
