mutation UpdateOfficer($input: UpdateOfficerInput!) {
  updateOfficer(input: $input) {
    officer {
      id
      officerNum
      firstName
      lastName
    }

    errors {
      field
      message
    }
  }
}
