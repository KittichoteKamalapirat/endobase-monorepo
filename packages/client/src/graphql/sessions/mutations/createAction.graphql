mutation createAction($input: CreateActionInput!) {
  createAction(input: $input) {
    action {
      id
      type
      passed
      sessionId
    }
    errors {
      field
      message
    }
  }
}
